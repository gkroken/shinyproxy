<project xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
         xmlns="http://maven.apache.org/POM/4.0.0"
         xsi:schemaLocation="http://maven.apache.org/POM/4.0.0 http://maven.apache.org/xsd/maven-4.0.0.xsd">
    <modelVersion>4.0.0</modelVersion>

    <groupId>eu.openanalytics</groupId>
    <artifactId>shinyproxy</artifactId>
    <version>3.2.4</version>
    <packaging>jar</packaging>
    <inceptionYear>2016</inceptionYear>

    <name>ShinyProxy</name>
    <description>deploy Shiny apps in an enterprise context</description>

    <organization>
        <name>Open Analytics NV</name>
        <url>https://www.shinyproxy.io/</url>
    </organization>

    <parent>
        <groupId>org.springframework.boot</groupId>
        <artifactId>spring-boot-starter-parent</artifactId>
        <version>3.5.14</version>
        <relativePath/>
    </parent>

    <properties>
        <project.build.sourceEncoding>UTF-8</project.build.sourceEncoding>
        <java.version>21</java.version>
        <maven.compiler.source>21</maven.compiler.source>
        <maven.compiler.target>21</maven.compiler.target>
        <resource.delimiter>&amp;</resource.delimiter>
        <!-- Dependency versions -->
        <containerproxy.version>1.2.4</containerproxy.version>
        <spring-boot.version>3.5.14</spring-boot.version>
        <datatables.version>1.13.5</datatables.version>
        <datatables-buttons.version>2.4.1</datatables-buttons.version>
        <datatables-responsive.version>2.2.7</datatables-responsive.version>
        <handlebars.version>4.7.9</handlebars.version>
        <sqlite-jdbc.version>3.50.3.0</sqlite-jdbc.version>
        <commons-lang.version>3.18.0</commons-lang.version>
        <aws-sdk.version>2.31.21</aws-sdk.version>
        <!-- Plugin versions -->
        <maven.license-plugin.version>4.6</maven.license-plugin.version>
        <maven.build-helper-maven.plugin.version>3.6.0</maven.build-helper-maven.plugin.version>
        <maven.dependency-check-plugin.version>12.1.7</maven.dependency-check-plugin.version>
    </properties>

    <distributionManagement>
        <snapshotRepository>
            <id>oa-nexus-snapshots</id>
            <name>OpenAnalytics Snapshots Repository</name>
            <url>https://nexus.openanalytics.eu/repository/snapshots</url>
        </snapshotRepository>
        <repository>
            <id>oa-nexus-releases</id>
            <name>OpenAnalytics Release Repository</name>
            <url>https://nexus.openanalytics.eu/repository/releases</url>
        </repository>
    </distributionManagement>

    <repositories>
        <repository>
            <id>oa-nexus-snapshots</id>
            <name>OpenAnalytics Snapshots Repository</name>
            <url>https://nexus.openanalytics.eu/repository/snapshots</url>
            <snapshots>
                <enabled>true</enabled>
            </snapshots>
            <releases>
                <enabled>false</enabled>
            </releases>
        </repository>
        <repository>
            <id>oa-nexus-releases</id>
            <name>OpenAnalytics Snapshots Repository</name>
            <url>https://nexus.openanalytics.eu/repository/releases</url>
            <snapshots>
                <enabled>false</enabled>
            </snapshots>
            <releases>
                <enabled>true</enabled>
            </releases>
        </repository>
    </repositories>

    <dependencies>
        <dependency>
            <groupId>eu.openanalytics</groupId>
            <artifactId>containerproxy</artifactId>
            <version>${containerproxy.version}</version>
        </dependency>
        <dependency>
            <groupId>eu.openanalytics</groupId>
            <artifactId>containerproxy</artifactId>
            <version>${containerproxy.version}</version>
            <type>test-jar</type>
            <scope>test</scope>
        </dependency>
        <dependency>
            <groupId>org.springframework.boot</groupId>
            <artifactId>spring-boot-starter-mail</artifactId>
        </dependency>
        <dependency>
            <groupId>org.webjars</groupId>
            <artifactId>datatables</artifactId>
            <version>${datatables.version}</version>
        </dependency>
        <dependency>
            <groupId>org.webjars</groupId>
            <artifactId>datatables-buttons</artifactId>
            <version>${datatables-buttons.version}</version>
        </dependency>
        <dependency>
            <groupId>org.webjars</groupId>
            <artifactId>datatables-responsive</artifactId>
            <version>${datatables-responsive.version}</version>
        </dependency>
        <dependency>
            <groupId>org.webjars.npm</groupId>
            <artifactId>handlebars</artifactId>
            <version>${handlebars.version}</version>
        </dependency>
        <dependency>
            <groupId>org.projectlombok</groupId>
            <artifactId>lombok</artifactId>
            <scope>provided</scope>
        </dependency>
        <dependency>
            <groupId>jakarta.servlet</groupId>
            <artifactId>jakarta.servlet-api</artifactId>
        </dependency>
        <dependency>
            <groupId>org.xerial</groupId>
            <artifactId>sqlite-jdbc</artifactId>
            <version>${sqlite-jdbc.version}</version>
        </dependency>
        <dependency>
            <groupId>org.apache.commons</groupId>
            <artifactId>commons-lang3</artifactId>
            <version>${commons-lang.version}</version>
        </dependency>

        <!-- Skald: content registry schema management. Versions are managed by
             spring-boot-starter-parent. Both are Apache License 2.0.
             flyway-database-postgresql is a separate module since Flyway 10, which split
             per-database support out of flyway-core.
             The PostgreSQL driver, spring-boot-starter-jdbc and HikariCP are already on the
             compile classpath transitively through containerproxy. -->
        <!-- Skald: S3-compatible object storage for bundles, build logs and rendered
             output (ADR-0007, spine #2 T4). Apache License 2.0, as is every artifact it
             resolves; flagged under Q4 before adding.

             THIS DOES NOT ADD THE SDK; IT PINS ONE THAT IS ALREADY HERE. ContainerProxy
             1.2.4 brings the whole 2.31.21 module set transitively, s3 INCLUDED, along with
             sso, sts, ecs and their protocol modules. Measured against the parent commit:
             33 awssdk artifacts before, 34 after, and the single new jar is
             url-connection-client. Nothing else was added, removed or version-changed.
             What this declaration buys is that an inherited dependency becomes an
             intentional one with a version this repository controls.

             VERSION IS PINNED TO THE ENGINE'S, NOT TO THE NEWEST, and that is the whole
             point. Declaring s3 at the then-latest 2.55.0 did not sit alongside the
             engine's set, it UPGRADED the engine's own s3 to 2.55.0 and left sso, sts, ecs
             and the protocol modules at 2.31.21, a mixed-version classpath with 2.55.0
             core underneath service modules compiled against 2.31.21. AWS publishes a BOM
             precisely to stop that, and mixing 2.x module versions is unsupported.
             AwsSdkClasspathTest fails if that ever stops holding.

             Upgrading the whole set to a newer SDK is a separate decision, because it
             changes what ContainerProxy runs with and not only what Skald runs with.

             NO EXCLUSIONS, deliberately. An earlier revision excluded netty-nio-client and
             apache-client from this dependency with a rationale about avoiding a second
             Netty. Both claims were wrong and were removed after measuring: Netty is
             already present via spring-boot-starter-reactor-netty, and those two HTTP
             clients arrive through ContainerProxy's sso rather than through s3, so
             excluding them here changed the resolved list not at all. The classpath
             therefore carries several HTTP implementations and the SDK's runtime discovery
             is ambiguous, so the client is selected EXPLICITLY in code. That is the fix;
             an exclusion that resolves to nothing is not. -->
        <dependency>
            <groupId>software.amazon.awssdk</groupId>
            <artifactId>s3</artifactId>
            <version>${aws-sdk.version}</version>
        </dependency>
        <dependency>
            <groupId>software.amazon.awssdk</groupId>
            <artifactId>url-connection-client</artifactId>
            <version>${aws-sdk.version}</version>
        </dependency>

        <dependency>
            <groupId>org.flywaydb</groupId>
            <artifactId>flyway-core</artifactId>
        </dependency>
        <dependency>
            <groupId>org.flywaydb</groupId>
            <artifactId>flyway-database-postgresql</artifactId>
        </dependency>
        <dependency>
            <groupId>org.springframework.boot</groupId>
            <artifactId>spring-boot-starter-test</artifactId>
            <scope>test</scope>
            <exclusions>
                <exclusion>
                    <groupId>org.junit.vintage</groupId>
                    <artifactId>junit-vintage-engine</artifactId>
                </exclusion>
            </exclusions>
        </dependency>
        <dependency>
            <groupId>org.junit.platform</groupId>
            <artifactId>junit-platform-launcher</artifactId>
            <scope>test</scope>
        </dependency>

        <!-- Skald: real PostgreSQL for registry tests. MIT licence, version managed by the
             Spring Boot parent. Needs a Docker socket, which `make test` already provides
             for upstream's container-backed integration tests. -->
        <dependency>
            <groupId>org.testcontainers</groupId>
            <artifactId>postgresql</artifactId>
            <scope>test</scope>
        </dependency>
        <dependency>
            <groupId>org.testcontainers</groupId>
            <artifactId>junit-jupiter</artifactId>
            <scope>test</scope>
        </dependency>

        <!-- Transitive dependencies updated for security -->
        <dependency>
            <groupId>org.webjars</groupId>
            <artifactId>swagger-ui</artifactId>
            <version>5.32.5</version>
        </dependency>
        <dependency>
            <groupId>org.apache.logging.log4j</groupId>
            <artifactId>log4j-api</artifactId>
            <version>2.25.4</version>
        </dependency>
    </dependencies>

    <build>
        <resources>
            <resource>
                <directory>src/main/resources</directory>
                <excludes>
                    <exclude>static/handlebars/node_modules/**</exclude>
                    <exclude>static/handlebars/.gitignore</exclude>
                    <exclude>static/handlebars/generate.sh</exclude>
                    <exclude>static/handlebars/*.handlebars</exclude>
                    <exclude>**/.gitignore</exclude>
                </excludes>
            </resource>
            <!-- Skald: the extraction bounds are declared once, in spec/, and READ at
                 startup rather than restated as constants (ExtractionLimits). That only
                 works if the file ships, so it is packaged here. Narrow on purpose: the
                 other spec/ documents are plan artifacts that nothing loads, and an
                 include list says which of them is code. -->
            <resource>
                <directory>spec</directory>
                <targetPath>skald/spec</targetPath>
                <includes>
                    <include>extraction-limits-v1.json</include>
                </includes>
            </resource>
        </resources>
        <plugins>
            <plugin>
                <groupId>org.springframework.boot</groupId>
                <artifactId>spring-boot-maven-plugin</artifactId>
                <executions>
                    <execution>
                        <id>repackage</id>
                        <goals>
                            <goal>repackage</goal>
                        </goals>
                        <configuration>
                            <classifier>exec</classifier>
                            <mainClass>eu.openanalytics.containerproxy.ContainerProxyApplication</mainClass>
                        </configuration>
                    </execution>
                    <execution>
                        <id>build-info</id>
                        <goals>
                            <goal>build-info</goal>
                        </goals>
                        <configuration>
                            <additionalProperties>
                                <containerProxyVersion>${containerproxy.version}</containerProxyVersion>
                            </additionalProperties>
                        </configuration>
                    </execution>
                </executions>
            </plugin>
            <plugin>
                <groupId>org.apache.maven.plugins</groupId>
                <artifactId>maven-compiler-plugin</artifactId>
                <configuration>
                    <source>${java.version}</source>
                    <target>${java.version}</target>
                    <annotationProcessorPaths>
                        <path>
                            <groupId>org.projectlombok</groupId>
                            <artifactId>lombok</artifactId>
                            <version>${lombok.version}</version>
                        </path>
                        <path>
                            <groupId>org.springframework.boot</groupId>
                            <artifactId>spring-boot-configuration-processor</artifactId>
                            <version>${spring-boot.version}</version>
                        </path>
                    </annotationProcessorPaths>
                </configuration>
            </plugin>
            <plugin>
                <groupId>com.mycila</groupId>
                <artifactId>license-maven-plugin</artifactId>
                <version>${maven.license-plugin.version}</version>

                <executions>
                    <execution>
                        <id>check-copyright-headers</id>
                        <phase>package</phase>
                        <goals>
                            <goal>check</goal>
                        </goals>
                    </execution>
                </executions>

                <configuration>
                    <licenseSets>
                        <licenseSet>
                            <header>LICENSE_HEADER</header>
                            <excludes>
                                <exclude>**/*.bak</exclude>
                                <exclude>**/*.conf</exclude>
                                <exclude>**/*.json</exclude>
                                <exclude>**/*.pem</exclude>
                                <exclude>**/*.properties</exclude>
                                <exclude>**/*.raml</exclude>
                                <exclude>**/*.sh</exclude>
                                <!-- Flyway migrations are data, not source, and are ours:
                                     upstream's header would assert Open Analytics copyright
                                     over them. Excluded here like the other config formats
                                     above; each migration carries its own explanatory
                                     comment block instead. -->
                                <exclude>**/*.sql</exclude>
                                <exclude>**/*.txt</exclude>
                                <exclude>**/*.xml</exclude>
                                <exclude>**/*.yaml</exclude>
                                <exclude>**/*.yml</exclude>
                                <exclude>**/*.handlebars</exclude>
                                <exclude>**/Dockerfile</exclude>
                                <exclude>.editorconfig</exclude>
                                <exclude>.gitignore</exclude>
                                <exclude>JenkinsfileSCM</exclude>
                                <exclude>LICENSE</exclude>
                                <exclude>LICENSE_HEADER</exclude>
                                <exclude>Makefile</exclude>
                                <exclude>dev/LICENSE_HEADER_SKALD</exclude>
                                <exclude>dev/LICENSE_HEADER_SKALD_SCRIPT</exclude>
                                <!-- Skald: our own dev tooling, covered by the third
                                     licenseSet below with a short header. -->
                                <exclude>dev/**/*.py</exclude>
                                <exclude>dev/**/*.js</exclude>
                                <exclude>README.md</exclude>
                                <exclude>docs/**</exclude>
                                <exclude>src/main/resources/configs/**</exclude>
                                <exclude>src/test/resources/**</exclude>
                                <!-- Skald: our own code carries our own copyright,
                                     asserted by the second licenseSet below. -->
                                <exclude>src/main/java/eu/openanalytics/shinyproxy/publisher/**</exclude>
                                <exclude>src/test/java/eu/openanalytics/shinyproxy/publisher/**</exclude>
                            </excludes>
                        </licenseSet>
                        <!-- Skald: code written for this fork. Applying upstream's header
                             here would assert Open Analytics copyright over code they did
                             not write; dropping the check entirely would lose the guarantee
                             on upstream files. Two sets keep both statements true. -->
                        <licenseSet>
                            <header>dev/LICENSE_HEADER_SKALD</header>
                            <includes>
                                <include>src/main/java/eu/openanalytics/shinyproxy/publisher/**</include>
                                <include>src/test/java/eu/openanalytics/shinyproxy/publisher/**</include>
                            </includes>
                        </licenseSet>
                        <!-- Skald: the dev suites and fixture generators. Ours, so the same
                             argument as above applies — but a twenty-one line block above a
                             file whose first line is its own explanation costs more than it
                             states, so these carry the two-line SPDX form instead. Nothing
                             here ships in the jar. -->
                        <licenseSet>
                            <header>dev/LICENSE_HEADER_SKALD_SCRIPT</header>
                            <includes>
                                <include>dev/**/*.py</include>
                                <include>dev/**/*.js</include>
                            </includes>
                        </licenseSet>
                    </licenseSets>
                    <aggregate>true</aggregate>
                    <strictCheck>true</strictCheck>
                    <properties>
                        <year>${current.year}</year>
                    </properties>
                </configuration>
            </plugin>
            <plugin>
                <groupId>org.apache.maven.plugins</groupId>
                <artifactId>maven-surefire-plugin</artifactId>
                <configuration>
                    <systemPropertyVariables>
                        <!-- ContainerProxy's own test helper (ShinyProxyInstance.TestConfiguration)
                             legitimately substitutes the proxyDispatcherService bean through a
                             @Primary @Bean, which DispatcherOverrideRegistrar cannot retarget.
                             Production fails closed when the override cannot be installed; the
                             test run tolerates it. The guarantee is not weakened:
                             LazyProxyDispatcherServiceTest boots its own context and asserts the
                             installed type directly. -->
                        <skald.dispatcher.require-override>false</skald.dispatcher.require-override>
                    </systemPropertyVariables>
                </configuration>
            </plugin>
            <plugin>
                <groupId>org.codehaus.mojo</groupId>
                <artifactId>build-helper-maven-plugin</artifactId>
                <version>${maven.build-helper-maven.plugin.version}</version>
                <executions>
                    <execution>
                        <id>timestamp-property</id>
                        <goals>
                            <goal>timestamp-property</goal>
                        </goals>
                        <phase>validate</phase>
                        <configuration>
                            <name>current.year</name>
                            <pattern>yyyy</pattern>
                            <locale>en-GB</locale>
                        </configuration>
                    </execution>
                </executions>
            </plugin>
            <plugin>
                <groupId>net.nicoulaj.maven.plugins</groupId>
                <artifactId>checksum-maven-plugin</artifactId>
                <version>1.9</version>
                <executions>
                    <execution>
                        <goals>
                            <goal>files</goal>
                        </goals>
                    </execution>
                </executions>
                <configuration>
                    <fileSets>
                        <fileSet>
                            <directory>${project.build.directory}</directory>
                            <includes>
                                <include>*.jar</include>
                                <include>*.pom</include>
                            </includes>
                            <excludes>
                                <exclude>*.sha256</exclude>
                                <exclude>*.md5</exclude>
                            </excludes>
                        </fileSet>
                    </fileSets>
                    <algorithms>
                        <algorithm>SHA-256</algorithm>
                        <algorithm>MD5</algorithm>
                    </algorithms>
                    <attachChecksums>true</attachChecksums>
                    <csvSummary>false</csvSummary>
                </configuration>
            </plugin>
        </plugins>
    </build>

    <profiles>
        <profile>
            <id>owasp-dependency-check</id>
            <build>
                <plugins>
                    <plugin>
                        <groupId>org.owasp</groupId>
                        <artifactId>dependency-check-maven</artifactId>
                        <version>${maven.dependency-check-plugin.version}</version>
                        <configuration>
                            <suppressionFiles>
                                <suppressionFile>owasp-suppression.xml</suppressionFile>
                            </suppressionFiles>
                        </configuration>
                        <executions>
                            <execution>
                                <goals>
                                    <goal>check</goal>
                                </goals>
                            </execution>
                        </executions>
                    </plugin>
                </plugins>
            </build>
        </profile>
    </profiles>

</project>
